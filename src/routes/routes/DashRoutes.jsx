import { NavBar } from "../../layout"
import { CountryContent } from "../../component"

const DashRoutes = () => {
    return <div className="content__principal">
        <NavBar/>
        <CountryContent/>
    </div> 
}

export default DashRoutes