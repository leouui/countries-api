import "./styles.css"

const NavBar = () => {
    return <nav className="navbar w-100">
        <div className="navbar__container w-page d-flex fx-between max-w-100">
            <div>
                Where in the world?
            </div>

            <div>
                Dark Mode
            </div>
        </div>
    </nav>
}

export default NavBar